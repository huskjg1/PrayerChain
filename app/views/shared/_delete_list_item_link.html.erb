<% if current_user?(list_item.user) %>
  <%= link_to "delete", list_item, method: :delete,
                                   data: { confirm: "Are you sure?" },
                                   title: list_item.content %>
<% end %>
